import Head from "next/head";
import Avatar from "@/components/Avatar";
import Writer from "@/components/Writer";
import bchess from "@/assets/bchess"
import Connect from "@/components/Connect";

export default function Home() {
    const text = `An enchanting fusion of strategy and blockchain technology transforms chess into a real-time competitive arena, bringing together the masters of the strategic world.`;

    return (
        <>
            <Head>
                <title>Chess</title>
                <meta
                    name="description"
                    content="Generated by create next app"
                />
                <meta
                    name="viewport"
                    content="width=device-width, initial-scale=1"
                />
            </Head>
            <Writer center textWrap={"balance"} fontSize={"32px"} text={text} color={"#fff"} minHeight={"80px"} />
            <Avatar loop={false} radius={"50%"} animation={bchess} width={400} height={400} />
             <Connect/>
        </>
    );
}
